# Web Title and Heading Design

## Goal

Update the application name so it accurately represents the current browser ASR benchmark, which compares selectable Moonshine models with Whisper Base.en.

## Approved copy

`Browser ASR Voice Navigation Benchmark`

## Scope

- Replace the browser document title in `index.html` with the approved copy.
- Replace the primary application H1 in `src/App.tsx` with the same approved copy.
- Leave the benchmark flow, subtitle, fallback error heading, styling, and all other content unchanged.

## Verification

- Confirm the browser tab title matches the approved copy.
- Confirm the visible primary H1 matches the approved copy.
- Run the existing test suite and production build.
